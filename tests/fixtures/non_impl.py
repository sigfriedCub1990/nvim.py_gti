# Completely unrelated file — must never appear in PyGTI results.


def speak():
    """A module-level function with the same name; not a method."""
    return "hello"


class Unrelated:
    def speak(self):
        return "unrelated"
